version: '2'

services:
    airflow:
      image: drupal:8.2-apache
      ports:
        - 8080:80
      restart: always

    postgres:
      image: postgres:9.6.9
      ports:
          - 8000:5432
      environment:
        POSTGRES_PASSWORD: password
        POSTGRES_USER: joeh
        POSTGRES_DB: test
      restart: always